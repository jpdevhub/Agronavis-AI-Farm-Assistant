# HF Spaces requirements — CPU Basic tier
# =========================================
# Standard pip install — works on HF CPU Basic (free tier)
# torch CPU wheel is ~700MB; installs in ~3-4 min on HF

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Environment
python-dotenv>=1.0.0

# Supabase SDK — pin exact chain so pip resolves websockets correctly
supabase==2.31.0
realtime==2.31.0
# websockets intersection: gradio-client needs <13, realtime needs >=11, asyncio submodule needs >=12
websockets>=12.0,<13.0

# Data validation
pydantic>=2.7.0

# ML — ZeroGPU comes with PyTorch pre-installed.
# torchvision is installed dynamically in app.py to avoid ZeroGPU validator bugs.
Pillow>=10.0.0

# CLIP (OOD guard for non-plant images)
transformers>=4.40.0

# Web search (disease wiki)
duckduckgo-search>=6.2.0
